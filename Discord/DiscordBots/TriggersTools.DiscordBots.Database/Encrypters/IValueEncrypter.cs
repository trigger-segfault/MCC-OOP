
namespace TriggersTools.DiscordBots.Database.Encrypters {
	/// <summary>
	/// An interface stating the class is a <see cref="ValueEncrypter{TModel, TProvider}"/> and
	/// that the constructor takes the arguments of <see cref="IByteEncrypter"/> and
	/// <see cref="EncryptDirection"/>.
	/// </summary>
	public interface IValueEncrypter {
		//IValueEncrypter(ByteConverter, EncryptionDirection);
	}
}
